# ================================================================
# GENERIC SESSION DEBRIEF — Katoa
# Generated by Standard Project Kit (skill standard-project-kit)
# ================================================================

runbook:
  name: Session Debrief
  version: "2.0"
  trigger:
    phrases: ["run session debrief"]

project_config:
  project_name: "Katoa"
  context_file: null
  framework_terms: null
  id_prefixes:
    architectural_decision: "AD-"
    open_question: "OQ-"
  output_location: "docs/debriefs/"

output_schema:
  session_id: "session-2026-08-19-001"
  date: "2026-08-19"
  status: "confirmed"

  summary:
    purpose: "Adopt the Standard Project Kit on Katoa and re-sync the THOR copy after a fresh git pull."
    outcomes:
      - "THOR copy /root/ref/katoa pulled fresh from origin — now on 8c9c3a4 (OF-parity handoff), 39 commits caught up."
      - "Verified live: katoa.org returns HTTP 200; site up."
      - "Recorded the first machine-readable session debrief for Katoa."

  architectural_decisions:
    - id: "AD-001"
      decision: "Treat /root/ref/katoa on THOR as the live/reference + handoff home. M3/M4 clones are working copies synced via git push/pull only."
      rationale: "Consistent with the Standard Project Kit rule across all repos (SherpaCarta/MotoPass/Satohash AD-001). Single source of truth on the ops brain; no copying folders between machines."
      status: "agreed"

  open_questions:
    - id: "OQ-001"
      question: "Katoa is a creator-content platform (OF-parity). Should the engagement features (likes/comments/PPV) be migrated off local seams to Nostr + LND backend in the same milestone, or as a separate later milestone?"
      context: "Handoff keeps likes/comments/PPV/seen as local seams until Nostr + the LND backend exist. Milestone sequencing affects scope of the next build."
      priority: "medium"
      status: "open"

  session_notes:
    deprecated_items: []
    corrections_to_prior_sessions: []
    other:
      - "Trial run of the Standard Project Kit on Katoa. First debrief for this repo; prior sessions recorded only in KIMI-HANDOFF.md prose."
